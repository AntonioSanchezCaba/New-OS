/*
 * include/gui/font.h — 8x16 bitmap font for AetherOS GUI
 *
 * The font data and text-rendering functions live in gui/font.c.
 * All drawing functions are also declared in gui/draw.h; this header
 * exists as a convenience alias for code that needs the font data
 * directly (e.g. the Aether Render Engine).
 */
#pragma once
#include <gui/draw.h>
